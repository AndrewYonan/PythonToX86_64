# PythonCompiler

Accepts plain ".txt" files and runs them as if they were python files (you MUST have a ".txt" extension on the input file).

# Run Instructions
clang -arch x86_64 asm/minimal.s -o minimal
./minimal
# Dependencies
brew install i686-elf-binutils qemu